  <div class="sidebar col-md-3 ml-4"><!--絞り込み部分テンプレート-->
      <h4 class="mt-3"><strong>絞り込み検索</strong></h4>
      <div class="category-search my-5">
        <h5 class="mb-4">【カテゴリ】</h5>
        <% categories.each do |category| %>
        <p class="mr-4">
          <%=link_to post_items_category_search_path(category.id) do %>
            <%= category.name%>
          <% end %>
        </p>
        <% end %>
      </div>

      <div class="prefucture-search">
        <h5 class="mb-4">【地域】</h5>
        <%= form_with url: post_items_prefecture_search_path, local: true, method: :get do |f| %>
          <%=f.collection_select :buy_prefecture_id,BuyPrefecture.all, :id, :name,{prompt: "--選択してください--"}%>
          <%=f.submit "検索" %>
        <% end %>
        <%=image_tag "japan_map.jpg",width: '100%', height: '100%',class:"my-5"%>
      </div>
    </div> <!--絞り込み部分テンプレートここまで-->

